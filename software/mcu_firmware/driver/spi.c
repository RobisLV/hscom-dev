/*
 * spi.c
 *
 *  Created on: 2017. gada 2. marts
 *      Author: Roberts
 */
#
#include "spi.h"

/***************************************************
 *
 **************************************************/
uint16_t SPI_A0_clock_phase(spi_setting spi_phase){
    if(spi_phase == ){
        MASK_SET(, );
    }
    else if(spi_phase == ){
        MASK_CLEAR(, );
    }
    else{
        return 1;
    }
    return 0;
}

/***************************************************
 *
 **************************************************/
uint16_t SPI_A0_clock_polarity(spi_setting spi_polarity){
    if(spi_polarity == ){
        MASK_SET(, );
    }
    else if(spi_polarity == ){
        MASK_CLEAR(, );
    }
    else{
        return 1;
    }
    return 0;
}

/***************************************************
 *
 **************************************************/
uint16_t SPI_A0_bit_order(spi_setting spi_bitorder){
    if(spi_bitorder == ){
        MASK_SET(, );
    }
    else if(spi_bitorder == ){
        MASK_CLEAR(, );
    }
    else{
        return 1;
    }
    return 0;
}

/***************************************************
 *
 **************************************************/
uint16_t SPI_A0_char_length(spi_setting spi_charlength){
    if(spi_charlength == ){
        MASK_SET(, );
    }
    else if(spi_charlength == ){
        MASK_CLEAR(, );
    }
    else{
        return 1;
    }
    return 0;
}

/***************************************************
 *
 **************************************************/
uint16_t SPI_A0_mode(spi_setting spi_mode){
    if(spi_mode == ){
        MASK_SET(, );
    }
    else if(spi_mode == ){
        MASK_CLEAR(, );
    }
    else{
        return 1;
    }
    return 0;
}

/*
if( == ){
    MASK_SET(, );
}
else if( == ){
    MASK_CLEAR(, );
}
else{
    return 1;
}
return 0;
*/
